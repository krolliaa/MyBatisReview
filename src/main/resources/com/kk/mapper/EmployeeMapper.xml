<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kk.mapper.EmployeeMapper">
    <select id="selectEmployeeByListTypeAliases" resultType="employee">
        select eid, emp_name empName, age, sex, email from t_emp;
    </select>
    <select id="selectEmployeeByListMapUnderscorpeToCamelCase" resultType="employee">
        select eid, emp_name, age, sex, email from t_emp;
    </select>
</mapper>